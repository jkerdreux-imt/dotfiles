# theme = "bogster"
theme = "jkx_tokyo_moon"
# theme = "tokyonight_moon"
# theme = "boo_berry"

[editor]
color-modes = true
bufferline = "always"
cursorline = true
popup-border = "all"
true-color = true
# rulers=[100]
end-of-line-diagnostics = "disable"
# gutters = ["diagnostics", "spacer", "line-numbers", "spacer", "diff"]
gutters = ["line-numbers", "spacer", "diff", "diagnostics", "spacer"]
completion-replace = true

[editor.inline-diagnostics]
cursor-line = "hint"
# other-lines = "error"

[editor.cursor-shape]
insert = "bar"
normal = "block"
select = "underline"

[editor.file-picker]
hidden = true

[editor.indent-guides]
render = false

[editor.statusline]
left = ["mode", "spinner", "version-control", "file-name", "read-only-indicator"]
right = ["diagnostics", "selections", "register", "position", "total-line-numbers", "file-type"]
separator = " >"


[editor.lsp]
auto-signature-help = false
display-messages = true
display-inlay-hints = false # disable by default but can be enable w/ c-t


[keys.normal]
# Cursor
h = "insert_mode"                                                                                                                                    
i = "move_visual_line_up"
k = "move_visual_line_down"                                                                                                                          
j = "move_char_left"                                                                                                                                
l = "move_char_right"
  
C-r = ":rla"
C-s = ":w"
C-q = ":q!"
C-x = ":buffer-close"
C-p = "file_picker_in_current_directory"

C-v = "paste_before"

C-h = "hover"
C-u = "goto_reference"
C-y = "goto_definition"

# navigation
C-k = "goto_next_paragraph"
C-i = "goto_prev_paragraph"
C-l = "move_next_word_start"
C-j = "move_prev_word_end"

C-a ="goto_line_start"
C-e ="goto_line_end"

# C-k = "half_page_down"
# C-i = "half_page_up"

# windows
C-pagedown = ":buffer-next"
C-pageup = ":buffer-previous"
"C-/" = ":sh kitty @ launch --cwd=$(dirname $(realpath %{buffer_name})) --no-response  --type=window"

# selection 
"C-." = "expand_selection"  # original alt-o / alt-i => collide w/ zellij
"C-," = "shrink_selection"

# A voir par la suite
C-b = ":echo %sh{git blame -L %{cursor_line},+1 %{buffer_name}}"

[keys.insert]
C-s = ":w"
C-q = ":q!"
C-v = "paste_before"
C-c = "toggle_comments" 

C-h = "hover"
C-u = "goto_reference"
C-y = "goto_definition"

# navigation
# C-k = "goto_next_paragraph"
# C-i = "goto_prev_paragraph"
C-l = "move_next_word_start"
C-j = "move_prev_word_end"
C-a ="goto_line_start"
C-e ="goto_line_end"


# windows
C-pagedown = ":buffer-next"
C-pageup = ":buffer-previous"
C-space = "completion"

[keys.select]
# cursor
i = "extend_visual_line_up"
k = "extend_visual_line_down"
j = "extend_char_left"
l = "extend_char_right"
C-k = "goto_next_paragraph"
C-i = "goto_prev_paragraph"
C-a ="goto_line_start"
C-e ="goto_line_end"

# create a new minor mode bound to 'r'
[keys.normal."r"]
# s = ":sh kitty @ launch --cwd=current  --type=overlay-main --no-response"
m = [":sh kitty @ launch --cwd=$(dirname $(realpath %{buffer_name})) --no-response --type=window make"]
c = [":sh kitty @ launch --cwd=current --no-response  --type=overlay-main copilot -i 'Lis le fichier %{buffer_name}' ",":rla",":redraw"]
g = [":sh kitty @ launch --cwd=current --no-response  --type=overlay-main lazygit",":rla",":redraw"]
j = ":open %sh{daily_journal}"


[keys.normal."t"]
h = ":toggle-option lsp.display-inlay-hints"
a = ":toggle-option auto-format"
t = ":set-language bash"
